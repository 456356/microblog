# 🐦 Microblog (FastAPI + PostgreSQL + Nginx)
Сервис микроблогов (аналог Twitter) для корпоративного использования.  
Бэкенд реализован на **FastAPI**, данные хранятся в **PostgreSQL**,  
статический фронтенд и медиафайлы отдаются через **Nginx**.

## 📋 Функциональные возможности
- Создание / удаление твита (текст до 280 символов, поддержка картинок)
- Подписка (`follow`) / отписка (`unfollow`) от других пользователей
- Постановка / снятие лайка (`like` / `unlike`) на твит
- Лента твитов от подписанных авторов, отсортированная по популярности (количество лайков, затем по дате)
- Загрузка медиафайлов (картинок) к твиту
- Просмотр профиля пользователя (подписчики, подписки)
- Авторизация по `api-key` в HTTP-заголовке

## 🏗 Tехнологии
| Компонент      | Технология                        |
|----------------|-----------------------------------|
| API            | FastAPI (Python 3.11)             |
| База данных    | PostgreSQL 15                     |
| Reverse proxy  | Nginx (отдача статики и `/media`) |
| Контейнеризация| Docker + Docker Compose           |
| Тесты          | pytest + pytest-asyncio           |

## 🏗 Архитектура MVC (Model-View-Controller)
- `models/` — бизнес-логика, raw SQL (`asyncpg`), Pydantic-схемы
- `models/common/` — конфигурация, БД, исключения, утилиты
- `controllers/` — FastAPI контроллеры (эндпоинты)

## 🚀 Развёртывание на Ubuntu 22.04 (пошаговая инструкция)
1. Запустите: `docker compose up -d --build`
2. Откройте: `http://localhost/` (Swagger: `http://localhost/docs`)
3. Тестовый `api-key`: `demo-key-1`, `demo-key-2` или `demo-key-3`
